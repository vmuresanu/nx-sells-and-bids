import { sharedUtilsInterfaces } from './shared-utils-interfaces';

describe('sharedUtilsInterfaces', () => {
  it('should work', () => {
    expect(sharedUtilsInterfaces()).toEqual('shared-utils-interfaces');
  });
});
